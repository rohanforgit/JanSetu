/**
 * Google Gemini API Primary Provider
 */
export const geminiProvider = {
  name: 'Google Gemini API (Primary)',
  analyzeIssue: async (issueData) => {
    // Contract definition for future integration
    throw new Error('Gemini Provider unconfigured in Phase 0 Architecture');
  }
};
